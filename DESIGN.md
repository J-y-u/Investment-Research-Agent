# 设计说明：实时数据 Agent 与可视化

本文讲清架构划分与关键取舍。

## 1. 为什么用 pi Agent SDK

题目要求 agent 内部体现 **tools / subagent / skill** 三者协作，这正是 pi 的原生概念：

| 概念 | pi 中的落地 |
|---|---|
| tool | 扩展内 `pi.registerTool(definition)`，参数用 typebox 校验 |
| subagent | `.pi/agents/*.md`：YAML frontmatter（工具/模型/思考级别）+ 系统提示，主 agent 经 `subagent` 工具 spawn |
| skill | `.pi/skills/<name>/SKILL.md`，按需加载的方法论 |

选型的另一理由：全 JS 单运行时，`node src/run.js` 即可编排，数据与产物层也都用 JS 库，无需跨语言。

## 2. 分层与依赖

```
market-data ──┐
              ├──→ analytics ──┬──→ viz-html
news-events ──┤                └──→ office-docs
              └──→ agent-core（编排，依赖全部）
```

- **纯函数层**（`analytics`）：输入数组、输出结果，不触网、不读时钟，易测试。
- **IO 层**（`data` / `artifacts`）：网络、文件，与纯函数严格分开。
- **编排层**（`agents/orchestrator.js`）：创建 pi 会话、加载扩展与子代理、按任务路由。

依赖单向无环；模块间通过明确的数据结构通信（Bar / Pivot / IndustryEvent / Match）。

## 3. 关键取舍

### 3.1 代码 vs LLM 的边界（最重要）

**确定性的事交给代码，需要判断的事才交给 LLM。**

- 取数、ZigZag 拐点、收益/波动/回撤/Sharpe/相关性、事件匹配、评级量化——全部代码确定。
- LLM 负责：任务规划、子代理分派、在代码给定窗口内的事件归因叙述、置信度判断。
- LLM **不计算、不篡改任何数字**：它拿到的是工具返回的结果。

理由：LLM 算数不可靠、不可复核；把数字固定在代码里，结论才可溯源、可测试。

### 3.2 双模式：agent 与 pipeline

- `agent`（默认）：LLM 驱动，展示 tools/subagent/skill 的协作。
- `--pipeline`：纯代码、无 LLM，可复现 / CI / 断网。
- 工具内部**复用同一批确定性函数**（`pipeline.js`），因此两种模式产出的数字必然一致。

这让"可复现"不依赖 LLM 的随机性，也方便快速验收。

### 3.3 事件库为何要分层

- Hacker News 检索**无法完整覆盖五年前**的事件（搜索 API 时效、相关性排序）。
- 因此事件以**人工核实的预置库**（`curated-events.js`）为准：日期与来源 URL 经实际检索核实，不靠记忆；HN 检索作为补充与佐证。
- 每个事件带 `valence`（对 NVDA 的基本面方向）与 `weight`（先验重要度）。

### 3.4 自包含 HTML（对应"安全与跨域"）

- Plotly JS 与数据全部**内联**进单文件：无 CDN、无外部脚本/样式，页面加载**零自动网络请求**。
- `file://` 双击可开、断网可用；规避了 CDN 供应链与跨域问题。
- 来源外链仅在用户点击后由浏览器主动发起，统一 `rel="noopener noreferrer"`。
- 代价：文件约 5 MB（内联 min 版 JS），换取零外联，值得。

### 3.5 工具经"扩展"注册（而非内联 customTools）

开发中实测发现：通过 `createAgentSession({customTools})` 内联传入的工具，**前台子代理不继承**（前台不加载父级 ambient 扩展）。因此工具改为由**项目扩展** `pi.registerTool` 注册，子代理通过 frontmatter 的 `subagentOnlyExtensions` 加载——这是 pi 子代理的正确加载路径。

### 3.6 编排者工具收窄

编排会话只保留 `subagent` + 8 个分析工具，**去掉 bash/edit/write/read**：

- 落实"编排者不做具体实现"的角色边界；
- 避免它越界改文件系统、或在基础设施报错时自行"修复"而陷入循环。

## 4. 事件-拐点匹配与评级

见 skill `event-attribution`，要点：

1. **硬约束窗口**：事件仅在拐点 ±10 个交易日内匹配，窗口外不配对。
2. **一一对应、贪心**：一个事件/拐点最多配对一次，按交易日 lag 最小优先。
3. **方向看事件 valence，不看拐点类型**：高点≠利空、低点≠利好。例如 DeepSeek 冲击把价格"打到低点"，事件方向仍是利空。
4. **评级（1–5）= max(先验 weight, 市场反应强度)**：
   - 市场反应取事件**后** 1/5/20 日中最大绝对收益（能捕捉 DeepSeek 那种单日 −17% 的即时冲击）；
   - 取较大者，避免 ChatGPT 这类长期重大事件被短期波动低估、也避免平淡事件被整段波段幅度夸大。
5. **冲突标注**：事件性质与事件后 5 日实际反应强烈相反时，标签注明、置信度降 low。
6. **宁空不凑**：配不上事件的拐点显式标"无重大已知事件（未归因）"。

## 5. 局限与后续方向（诚实披露）

1. **非超额收益**：未引入大盘对照时，事件后收益包含大盘等非事件因素，属"时间关联的市场反应"，时间相关≠因果。本次 7 条匹配中多 数存在方向冲突，已逐条标注。
   - 增强方向：加入 SPY/QQQ 计算事件窗口**超额收益（CAR）**，剥离大盘 beta。
2. **窗口参数敏感**：个别匹配 lag=10 紧贴窗口边界，窗口取值会影响结果；阈值已集中在 `config.js`。
3. **内联库的休眠代码**：Plotly 内联库自带 map 轨迹的 CDN/fetch 字符串，本页不会触发，但静态扫描可见。
4. 数据口径：Yahoo 返回已拆股复权、未做股息复权；连续合约（GC=F）存在换月。

## 6. Web 应用（第二形态）与通用化

### 6.1 为什么 Web 端不复用完整 coding-agent

CLI 端用 pi `@earendil-works/pi-coding-agent`（含 agent/subagent 编排）。Web 端改为直接用 **`@earendil-works/pi-ai`**（模型抽象层）+ 自建一个**紧凑受控 agent loop**，原因：

- Web 场景面向不可信输入，需要**最小权限**：只要 5 个投研工具，不需要 bash/edit/write。
- 用 pi-ai 可自己掌控工具白名单、流式事件、输入校验与密钥边界。
- `createProvider()` + `openAICompletionsApi()` 能以 OpenAI 兼容协议接入任意 provider（DeepSeek / 火山方舟 ARK），密钥只在后端。

两形态**共用同一套确定性函数**（`pipeline.js` / `analytics/`），因此数字口径一致。

### 6.2 通用化：从“英伟达样例”到“任意标的”

事件研究原本只支持 NVDA（curated 事件库）。通用化引入 **事件源路由**（`data/event-source.js`）：

- 有 curated 深度事件库的标的（NVDA）→ 用人工核实的事件（日期/来源/valence/weight）。
- 其他任意标的 → **Hacker News 动态检索**（免费、可溯源），无先验方向，结论完全由“事件后实际收益”支撑。

同时：
- 报告产物**参数化**：`excel.js`/`ppt.js`/`word.js` 从写死的 gold/btc 改为通用的 A/B 两标的（标签来自 ticker），`runComparePipeline(s1, s2)` 支持任意组合。
- 事件超出行情区间的条目被过滤，避免时间轴被拉宽。

### 6.3 安全边界（Web）

| 层 | 措施 |
|---|---|
| 输入 | 长度限制（1500 字）、JSON 校验、标的代码白名单正则 |
| 提示 | 系统提示限定投研、禁止越权与投资建议、强制中文 |
| 工具 | 仅 5 个投研工具，无执行/文件能力 |
| 输出 | 产物文件名白名单 + 扩展名白名单，路径遍历过滤 |
| 传输 | 仅监听 127.0.0.1、安全响应头、SSE、密钥不入前端 |
| 资源 | 频率限制（每 IP 8 次/分）、请求体上限 |

### 6.4 可视化范式修正（工业界做法）

第一版 HTML 画满了拐点竖线，噪声大且与事件割裂。参照 AnyChart / ApexCharts / TradingView / Bloomberg 事件可视化实践，改为：

- 底部**事件标记带**（形状区分类别、颜色区分方向），默认无竖线；
- **点击事件才高亮**其 ±窗口（竖线 + 淡色矩形）；
- 事件扩充到 30 个（20 个季度财报 + 10 个重大产品/市场事件），并按“事件后实际收益”给评级；
- 顶部 KPI 条 + 可筛选事件卡片（全部 / ≥4★ / 仅财报 / 产品市场）。

## 7. 工程方法

遵循个人《我的 AI 开发工作流》：spec → plan → TDD/incremental → review → ship，关键把关点（规格核对、架构、验收）显式记录；开发过程中的核心问题（事件方向误判、子代理工具加载、产物链接覆盖、事件锚点）均通过根因分析定位修复，过程见 README「AI 开发过程」。
